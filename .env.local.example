# Content is fetched at build time inside Server Components, so these are not
# read in the browser and carry no NEXT_PUBLIC_ prefix. Prefixing them would
# put the project id in the client bundle for no benefit.
SANITY_PROJECT_ID=afeko6yc
SANITY_DATASET=production
SANITY_API_VERSION=2026-08-16
